# Finance Research Report Template

**Symbol:** {{ symbol }}  
**Date:** {{ date }}  
**Report Type:** Snapshot (1-2 pages)

---

## Executive Summary

{% if change_pct is not none %}
**Price Action:** {{ "▲" if change_pct >= 0 else "▼" }} {{ change_pct|abs|round(2) }}% ({{ "gain" if change_pct >= 0 else "loss" }})
{% else %}
**Price Action:** No change data available
{% endif %}
**Current Price:** ${{ current_price|round(2) if current_price else 'N/A' }}
**Market Cap:** ${{ "{:,}".format(market_cap) if market_cap else 'N/A' }}
**Volume:** {{ "{:,}".format(volume) if volume else 'N/A' }}

### Technical Snapshot

| Indicator | Value | Interpretation |
|-----------|-------|----------------|
| RSI(14) | {{ rsi }} | {{ "Overbought" if rsi > 70 else "Oversold" if rsi < 30 else "Neutral" }} |
| SMA 50 | ${{ sma_50 }} | {{ "Bullish" if current_price > sma_50 else "Bearish" if sma_50 else "N/A" }} |
| SMA 200 | ${{ sma_200 }} | {{ "Long-term uptrend" if current_price > sma_200 else "Long-term downtrend" if sma_200 else "N/A" }} |
| MACD Histogram | {{ macd_hist if macd_hist is defined else 'N/A' }} | {{ "Bullish" if macd_hist > 0 else "Bearish" if macd_hist else 'N/A' }} |

### Sentiment Analysis

**Overall News Sentiment:** {{ sentiment_summary|title }}
{% if news and news|length > 0 %}
**Recent Headlines:**
{% for article in news %}
- {{ article.title }} — {{ article.source }}
{% endfor %}
{% else %}
*No recent news found.*
{% endif %}

---

## Chart

{% if include_charts %}
![Price Chart](charts/{{ symbol }}_{{ date }}.png)
{% endif %}

---

## Key Takeaways

{% if rsi > 70 %}
- ⚠️ OVERBOUGHT: RSI at {{ rsi }} suggests potential pullback
{% elif rsi < 30 %}
- 🟢 OVERSOLD: RSI at {{ rsi }} indicates possible bounce
{% endif %}

{% if sma_50 and sma_200 %}
{% if sma_50 > sma_200 %}
- ✅ GOLDEN CROSS: 50 SMA above 200 SMA (bullish trend)
{% else %}
- ⚠️ DEATH CROSS: 50 SMA below 200 SMA (bearish trend)
{% endif %}
{% endif %}

{% if sentiment_summary == 'positive' %}
- 📈 Positive news sentiment → potential upside catalyst
{% elif sentiment_summary == 'negative' %}
- 📉 Negative news sentiment → near-term pressure
{% endif %}

---

## Next Steps

- [ ] Watch for {{ rsi > 70 and 'RSI to cool below 65' or (rsi < 30 and 'RSI to recover above 35' or 'price confirmation at support/resistance') }}
- [ ] Monitor next earnings date: ____
- [ ] Review insider/inst. activity if available
- [ ] Set price alerts on key levels

---

*Report generated by finance-research-agent on {{ generated_at }}. Not financial advice. DYOR.*
